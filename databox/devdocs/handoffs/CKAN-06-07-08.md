# Handoff — CKAN-06, CKAN-07, CKAN-08

**Prompts:** CKAN-06 (CKAN Action API client), CKAN-07 (SHACL validation), CKAN-08 (RDF→tabular translation)
**Status:** complete (typecheck clean, 85 tests total)
**Date:** 2026-07-25

## Files created

### CKAN-06
- `ckan-bridge/src/ckan-action-client.ts` — `HttpCkanActionClient`:
  - POST /api/3/action/{action} with Authorization header
  - API token loaded from file (lazy, reloadable)
  - Convenience methods: packageShow, packageCreate, packageUpdate, datastoreCreate, datastoreUpsert
  - `CkanApiError` with action name and CKAN error payload
  - Fail closed on HTTP errors, network errors, success=false, missing token file
- `ckan-bridge/test/ckan-action-client.test.ts` — 12 tests

### CKAN-07
- `ckan-bridge/src/shacl-validator.ts` — `FileShaclValidator`:
  - Loads SHACL shapes from ConfigMap-mounted file
  - `validate()` and `validateContent()` methods
  - `contentDigest()` for idempotency (SHA-256)
  - `mapShaclReport()` helper for integration
  - `ShaclEngineError` with error codes
- `ckan-bridge/test/shacl-validator.test.ts` — 8 tests

### CKAN-08
- `ckan-bridge/src/rdf-translator.ts` — `ProgramProfileRdfTranslator`:
  - `translate()` and `translateQuads()` methods
  - Anonymisation: stripped, hashed, generalised (per program profile rules)
  - Web Civics ontology boundary: Civics (natural person) data auto-stripped, Civic (institutional) data included
  - Anonymisation decisions recorded as evidence (ADR-0011)
  - `isCivicsProperty()` / `isCivicProperty()` helpers
  - Unmapped properties skipped, single values unwrapped from arrays
- `ckan-bridge/test/rdf-translator.test.ts` — 13 tests

## Test results

```sh
npx tsc --noEmit  # clean
npx jest          # 7 suites, 85 tests, all passed
```
