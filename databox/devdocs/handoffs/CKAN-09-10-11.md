# Handoff — CKAN-09, CKAN-10, CKAN-11

**Prompts:** CKAN-09 (webhook handler), CKAN-10 (publication pipeline), CKAN-11 (integration)
**Status:** complete (typecheck clean, 102 tests total)
**Date:** 2026-07-25

## Files created

### CKAN-09
- `ckan-bridge/src/webhook-handler.ts` — `HmacWebhookHandler`:
  - HMAC-SHA256 signature verification with constant-time comparison
  - Secret loaded from file (lazy)
  - Idempotent: deduplicates by event id (type:datasetId:timestamp)
  - Event routing via callback registration (`on()`)
  - `WebhookError` with error codes
- `ckan-bridge/test/webhook-handler.test.ts` — 8 tests

### CKAN-10
- `ckan-bridge/src/publication-pipeline.ts` — `InMemoryPublicationPipeline`:
  - Full Pod→CKAN flow: read → validate → translate → publish → ledger
  - Idempotency key: sha256(podResourceIri:contentDigest)
  - SHACL validation failure → quarantine (never publish)
  - Bounded retries with max-retries → quarantine
  - Disclosure ledger records every publication event
  - `PublicationError` with error codes
- `ckan-bridge/test/publication-pipeline.test.ts` — 6 tests (with mocked Pod fetch)

### CKAN-11
- `ckan-bridge/src/bridge-service.ts`:
  - `CkanBridgeServiceImpl` — wires all components
  - `InMemoryDisclosureLedger` — records publications, corrections, anonymisations
  - `loadBridgeConfig()` — loads from env + file paths
  - HTTP server with /health, /webhook, /publish endpoints
  - Webhook callback registration for reconciliation (CKAN-13)
- `ckan-bridge/test/bridge-service.test.ts` — 3 tests

## Test results

```sh
npx tsc --noEmit  # clean
npx jest          # 10 suites, 102 tests, all passed
```
